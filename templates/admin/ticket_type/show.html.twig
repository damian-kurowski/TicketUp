{% extends 'base.html.twig' %}

{% block title %}TicketType{% endblock %}

{% block body %}
    <h1>TicketType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ticket_type.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ ticket_type.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ ticket_type.quantity }}</td>
            </tr>
            <tr>
                <th>AvailableQuantity</th>
                <td>{{ ticket_type.availableQuantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_ticket_type_index') }}">back to list</a>

    <a href="{{ path('app_admin_ticket_type_edit', {'id': ticket_type.id}) }}">edit</a>

    {{ include('admin/ticket_type/_delete_form.html.twig') }}
{% endblock %}
